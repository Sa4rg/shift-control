/**
 * Shift Control design system — border radius tokens.
 *
 * Import from "@/src/theme" rather than from this file directly.
 */
export const radius = {
  sm: 8,
  md: 10,
  lg: 12,
  xl: 16,
  pill: 999,
} as const;
